# ========================================
# Secure Chat Web - Environment Variables
# ========================================
# Copy this file to .env and configure for your deployment
# For M2M networks, all external services should point to local servers

# ========================================
# Database Configuration
# ========================================
# MySQL or TiDB connection string
# Format: mysql://username:password@host:port/database
# Example (local): mysql://root:password@localhost:3306/secure_chat
# Example (remote): mysql://user:pass@192.168.1.100:3306/secure_chat
DATABASE_URL=mysql://user:password@localhost:3306/secure_chat

# ========================================
# Authentication & Security
# ========================================
# JWT secret for session tokens (generate with: openssl rand -base64 32)
JWT_SECRET=your-secure-random-string-here

# Admin credentials for initial setup
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# ========================================
# Application Configuration
# ========================================
# Application title (displayed in UI)
VITE_APP_TITLE=Secure Chat Web

# Application logo path (relative to public folder)
VITE_APP_LOGO=/logo.svg

# Server port
PORT=3000

# Node environment (development | production)
NODE_ENV=production

# ========================================
# Jitsi Meet Configuration (Video Calls)
# ========================================
# Jitsi Meet server domain
# For M2M networks: Use local IP address (e.g., 192.168.1.100:8443)
# For internet: Use public Jitsi server (e.g., meet.jit.si)
# For self-hosted: Use your domain (e.g., jitsi.yourdomain.com)
VITE_JITSI_DOMAIN=meet.jit.si

# ========================================
# Optional: OAuth Configuration
# ========================================
# Only needed if using external OAuth providers
# For M2M networks, use built-in username/password authentication
# OAUTH_SERVER_URL=
# VITE_OAUTH_PORTAL_URL=
# VITE_APP_ID=

# ========================================
# Optional: Analytics
# ========================================
# Only needed if you want to track usage analytics
# VITE_ANALYTICS_ENDPOINT=
# VITE_ANALYTICS_WEBSITE_ID=

# ========================================
# Optional: External APIs
# ========================================
# Only needed if using external services (not required for M2M)
# BUILT_IN_FORGE_API_URL=
# BUILT_IN_FORGE_API_KEY=
# VITE_FRONTEND_FORGE_API_URL=
# VITE_FRONTEND_FORGE_API_KEY=

# ========================================
# Notes for M2M Network Deployment
# ========================================
# 1. Set DATABASE_URL to your local MySQL server IP
# 2. Set VITE_JITSI_DOMAIN to your local Jitsi server IP:port
# 3. Generate a strong JWT_SECRET
# 4. Change ADMIN_PASSWORD immediately
# 5. All services should be accessible via local network IPs
# 6. No internet connection required once configured
